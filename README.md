# devpulse-games
Collection of modular React/TypeScript mini-games for DevPulse. Each game is a standalone component with its own logic, sounds, and styling, ready to be plugged into the DevPulse terminal app.
