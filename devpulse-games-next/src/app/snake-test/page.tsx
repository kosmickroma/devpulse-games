"use client";

import SnakeGame from "@/components/games/SnakeGame";

export default function SnakeTestPage() {
  return (
    <main className="min-h-screen bg-dark-bg flex items-center justify-center">
      <SnakeGame />
    </main>
  );
}
